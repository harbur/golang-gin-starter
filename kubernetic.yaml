images:
  - path: Dockerfile
    context: .
    image: woven-computing-234012/golang-demo
    tags:
      - ${GIT_BRANCH}
      - ${GIT_COMMIT_SHORT}
      - ${GIT_BRANCH}-${BUILD_NUMBER}
charts:
  - charts/golang-demo
environments:
  - name: golang-demo
    namespace: kc-demo
    path: charts/golang-demo
