images:
  - name: woven-computing-234012/golang-demo
    path: Dockerfile
    context: .
    tags:
      - ${GIT_BRANCH}
      - ${GIT_COMMIT_SHORT}
      - ${GIT_BRANCH}-${BUILD_NUMBER}
charts:
  - charts/golang-demo
releases:
  - name: golang-demo
    namespace: kc-demo
    path: charts/golang-demo
    values:
    - releases/secrets.yaml
