docker:
  - path: Dockerfile
    context: .
    image: eu.gcr.io/woven-computing-234012/golang-demo
    tags:
      - ${GIT_BRANCH}
      - ${GIT_COMMIT_SHORT}
      - ${GIT_BRANCH}-${BUILD_NUMBER}
environments:
  - name: golang-demo
    namespace: kc-demo
    path: charts/golang-demo
