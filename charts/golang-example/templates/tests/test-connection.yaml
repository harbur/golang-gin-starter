apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "golang-example.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "golang-example.name" . }}
    helm.sh/chart: {{ include "golang-example.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "golang-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
